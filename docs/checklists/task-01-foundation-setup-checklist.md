# Task 01 Checklist

- [x] `RECORD_AUDIO`, `ACCESS_FINE_LOCATION`, `ACCESS_COARSE_LOCATION` 필요 여부를 반영한다.
- [x] `MainActivity.kt`의 런타임 권한 요청 목록을 확장한다.
- [x] `app/build.gradle.kts`에 네트워크/직렬화 의존성을 추가한다.
- [x] `BuildConfig` 또는 동등한 경로로 `openai_api_key`를 읽을 수 있게 한다.
- [x] `BuildConfig` 또는 동등한 경로로 `google_maps_api_key`를 읽을 수 있게 한다.
- [x] `audio`, `ai`, `maps`, `location`, `navigation` 패키지 골격을 만든다.
- [x] 이후 태스크가 사용할 공용 모델 파일을 최소 단위로 만든다.
- [x] 기존 DAT 연결과 디스플레이 연결 빌드가 깨지지 않는지 확인한다.
