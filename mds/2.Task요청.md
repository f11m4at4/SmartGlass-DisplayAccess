## 배경
`docs/PLAN.md`에 프로젝트 계획이 작성되어 있다.

## 요구사항
`docs/PLAN.md`의 계획을 `docs/tasks`에 Task 단위로 분리해서 MD 파일로 만들어줘.
- `docs/checklists` 폴더에 task 별 체크리스트가 작성된 md 파일을 만들어줘.
- 너무 복잡한 구현 단계는 이해하기 어려워. 프로타입으로 핵심 내용을 검증하고 싶은거니까 체크해서 테스크 설계가 복잡해 지지 않도록 해줘. 